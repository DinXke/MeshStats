__pycache__/
*.pyc
.env
mosquitto/passwd
mosquitto/acl
server/data/
# Download cache of tools/build_borders.py: several MB of upstream Natural Earth,
# of which the generated app/data/borders.json already carries the useful part.
server/tools/_ne_*.geojson
.venv/
venv/
# Zelf-gehoste kaart-assets: door pmtiles-extract/Protomaps gegenereerd, ~1,5 GB.
# Leeft als read-only bind-mount op de server (docker-compose /tiles), niet in git.
tiles/
